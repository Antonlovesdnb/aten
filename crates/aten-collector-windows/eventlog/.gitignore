# Build artifacts produced by build-manifest.ps1 (mc.exe -> rc.exe -> link.exe).
# Not committed — rebuild from aten.man with build-manifest.ps1. Only the
# manifest and the build script are source.
aten.h
aten.rc
aten.res
*.bin
*.BIN
aten_events.dll
